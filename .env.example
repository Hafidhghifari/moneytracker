# Salin file ini menjadi .env: cp .env.example .env
# Port host 5432 — ubah di docker-compose.yml dan di sini bila bentrok dengan
# Postgres lokal yang sudah berjalan.
DATABASE_URL="postgresql://moneyhist:moneyhist@localhost:5432/moneyhist?schema=public"
